export default class DOMes {
	constructor(d) {
		this.d = d || false
	}

	dispatch() {
		const e = {}
		document.querySelectorAll("*").forEach(({ tagName: n }) => {
			e[n] = e[n] ? e[n] + 1 : 1
		})

		return this.d ? console.table(e) : e
	}
}
